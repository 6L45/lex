# Core (sync XML + DB)
beautifulsoup4>=4.12.0
lxml>=5.0.0
pgvector>=0.3.0
psycopg[binary]>=3.2.0
python-dotenv>=1.0.0
requests>=2.31.0

# Embeddings — installe uniquement le provider que tu utilises:

# openai (LEX_EMBEDDING_PROVIDER=openai)
langchain-openai>=0.2.0

# cohere (LEX_EMBEDDING_PROVIDER=cohere)
# pip install langchain-cohere

# sentence-transformers local (LEX_EMBEDDING_PROVIDER=sentence-transformers)
# pip install sentence-transformers
